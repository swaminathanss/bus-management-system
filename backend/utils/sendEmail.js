const sendEmail = async (to, subject, text) => {
    console.log('--- EMAIL (placeholder, not actually sent) ---');
    console.log('To:', to);
    console.log('Subject:', subject);
    console.log('Body:', text);
    console.log('-----------------------------------------------');
};
module.exports = sendEmail;